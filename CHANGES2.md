# تغییراتِ این نسخه

۲ فایل عوض شده: `align_batch.py` و `README.md`. جای‌گزین کنید (همان مسیرِ `align_engine/`).

## اجرا بدونِ آرگومان، با یک پوشه‌ی `share`

به‌جایِ سه پوشه‌ی جدا (`--audio_dir` / `--text_dir` / `--out_dir`)، حالا فقط کافی‌ست یک پوشه‌ی `share` کنارِ خودِ `align_batch.py` بسازید و صدا+متن را با هم داخلش بگذارید:

```
align_engine/
  align_batch.py
  share/
    فایل.mp3
    فایل.docx  (یا .txt)
```

و فقط این را اجرا کنید:
```
python align_batch.py
```

خروجی خودکار در `share/output` نوشته می‌شود.

**سازگاریِ عقب‌رو:** اگر بخواهید هنوز پوشه‌های جدا داشته باشید، آرگومان‌های قدیمی (`--audio_dir`, `--text_dir`, `--out_dir`) هنوز کار می‌کنند — هرکدام را بدهید همان را override می‌کند، بقیه از `share` می‌آیند. یک `--share_dir` جدید هم اضافه شده اگر خواستید خودِ پوشه‌ی `share` را هم جای دیگری بگذارید.

منطقِ resolve-کردنِ پوشه‌ها را (بدونِ نیاز به مدل/torch) با چند سناریو تست کردم — کارِ درستی هم برای حالتِ بدونِ آرگومان، هم override جزئی، هم override کامل انجام می‌دهد.
